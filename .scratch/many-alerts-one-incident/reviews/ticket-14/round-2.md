# Ticket 14 — round 2 accepted

The human replied “agree” to all seven recommendations in the conversation after the sequential reviews. All seven are accepted. The original question wording below is preserved as the approval record; the authoritative consolidated mechanics are in ticket 14’s Answer. D1-D6 remain agreed, with D6’s exception narrowed by item 7.

1. **Grouping timers:** retain `group_wait: 10s` and `group_interval: 10s`, with the agreed 10m repeat? **Recommend yes.** These are the committed grouping timers; no new scheduler result justifies changing them. Evidence: `jira/notification-policy.yaml:19-21`.

2. **Exact repeats:** should a changed `values` object make an otherwise identical set of Fingerprints/statuses eligible for a Run? **Recommend yes:** compare sorted Fingerprint/status/values records per source group; suppress identical records. This preserves numerical trends when values change, but makes no promise of a fixed number of Runs. Evidence: `grafana.md`, objection 2; current Skill `skill/incident-sync/SKILL.md:114-120`.

3. **Coalescing:** keep only the latest accepted delivery for each Fingerprint in the one pending input, retaining source group identity and arrival order; absence from a later delivery never means Resolved? **Recommend yes.** Apply D6's Firing-before-Resolved rule only after duplicate Fingerprints have been reduced to their latest state. Evidence: `grafana.md`, objection 5; `jira/report.md`, objection 4. This defines the local arrival-order contract, not protection against out-of-order upstream delivery.

4. **Ambiguous or stale Matches:** use a 30-minute Incident-created window, read from Jira's clock, for both exact-Fingerprint admission and cascade candidates; if several qualify, read their Reports and accept only a uniquely evidence-supported Match; otherwise create a separate Incident with an ambiguity note? **Recommend yes for this 30-minute demo.** This favors a visible duplicate over silently combining unrelated Faults. The 30 minutes is a proposed policy value, not a measured threshold. A stale Incident is left for human disposition. Evidence: `stage.md`, objection 4; `completeness.md` Match clause.

5. **Wrong Match or duplicate correction:** may the Run flag a suspected error and propose the correction, while a human owns moving membership, merging duplicates and deciding any Severity correction? **Recommend yes for the demo.** Record linked correction notes and the evidence; do not silently move labels, merge or lower Severity. While a correction is pending, the Run must not automatically Complete the affected Incident. This accepts a visible unresolved item rather than an unaudited repair. Evidence: `docs/adr/0006-one-incident-per-fault-and-match-is-a-judgment.md:9`; `jira/report.md`, objection 5.

6. **Later Report edits:** append new evidence and explicit correction entries, preserving earlier claims, rather than silently rewriting them? **Recommend yes.** Ticket 16 decides the ADF/storage and how a current view is presented; this ticket decides the preservation rule. Evidence: ticket 14 Question :9; `docs/adr/0006-one-incident-per-fault-and-match-is-a-judgment.md:10`.

7. **Completion exception:** keep the escape hatch only as an explicit human-authorized forced completion, naming unresolved Fingerprints, rather than allowing one resolved group to override known-Firing accepted members automatically? **Recommend yes.** Normal completion still requires every accepted member to be Resolved. This narrows the agreed exception and therefore needs your answer. Evidence: `jira/capture-analysis.txt:1-24`; `stage.md`, objection 3.

Implementation consequences already supported by the CLI/source evidence: create/preserve cascade and fp labels; add fp labels without replacing the set; maintain per-member current state and clear a resolved marker on re-fire; ratchet Severity and Urgency together after membership is accepted. These are accepted spec requirements, not implemented changes. Live OPS editability and matched-Run timing remain unverified acceptance gates; ticket21 owns timeout handling.
