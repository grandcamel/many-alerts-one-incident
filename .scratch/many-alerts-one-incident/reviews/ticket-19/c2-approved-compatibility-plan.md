# Approved C2 compatibility preparation — 2026-09-21

User authorization: "C2 approved. Continue" supersedes the earlier no-runtime boundary for C2. It does not authorize a fifth C1 attempt, unrelated resources, qualification model calls, publication, or relaxed privileges.

Plan before source edits:
1. Parent: record authorization, inspect/pull the exact pinned C2 amd64 image once with a bounded pull, inspect Docker context/capacity and existing resources, and prepare a concrete first-stage card. Preserve existing images and resources. Pull alone is not backend acceptance.
2. Terra lane: durable exact-ID engine resource receipt ledger for the existing pure ownership planner; explicit intent before create, successful ID persistence before later side effects, fail-closed no name adoption, partial cleanup inventory, meaningful offline fault tests. No actual Docker calls by worker.
3. Parent: bounded first-stage compatibility harness for four direct-binary backend containers and one internal network. Use existing compiled configuration and materialization, a private ephemeral Grafana bootstrap secret, no published ports/host networking/host Docker socket, no additional application components. Inspect exact image ENV and compiled controls, effective user/limits/tmpfs/mounts, process/listener state and bounded local health responses through engine exec. No seed, Viewer token, gateway/Forwarder/Run or native/model session in this stage. Persist exact resource identities and capture bounded logs, then remove only receipt-backed IDs and verify absence. Preserve base images. A setup incompatibility stops the stage; no silent retry/reseed/privilege relaxation.
4. Sol: independent adversarial card/harness/receipt review before launch, then outcome review. Earlier external reviewer failures remain distinct; do not retry/reroute rejected packets.
5. Parent: full prototype suite and focused lint, source/hash freeze, local commits, then exactly one reviewed compatibility attempt under the user's current approval. Keep setup<=600s, diagnostics<=600s, cleanup<=120s, individual calls clipped to phase deadline; evidence<=2GiB, per-backend logs<=4MiB, private directory/file modes0700/0600, retention30days. Stop before ingestion while compatibility/account/native-policy gates are unresolved. Summarize actual results, cleanup and remaining C2 work honestly.
