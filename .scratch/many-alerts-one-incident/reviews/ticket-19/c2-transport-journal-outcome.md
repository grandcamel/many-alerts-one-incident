# C2 transport and durable journal source outcome — 2026-09-21

**Source preparation only; C2 runtime remains NOT RUN.** The user directed continuation after the compiler/driver batch. Two Terra lanes prepared the direct HTTP adapter and ingestion journal, with the parent completing integration tests and Sol performing adversarial review. The initial journal draft was incomplete and was replaced before validation; no partial implementation was treated as accepted. The [plan](c2-transport-journal-plan.md) fixes this batch's boundaries.

The adapter regenerates the complete compiled configuration and validates all request fields before opening one direct numeric IPv4 socket. Exact routes and caller-header shapes are allowed; the adapter is not a standalone authorization proxy or a replacement for the driver's frozen seed checks. Connect, partial writes, fragmented headers and response bodies share one absolute deadline. Proxy settings, authentication, redirects, reconnects and request retries are absent. Responses use a deliberately narrow HTTP/1.1 subset: fixed Content-Length, bounded headers/body, no duplicate headers, interim status, transfer encoding or compression. Chunked and close-delimited responses remain unsupported; actual pinned-backend compatibility is unmeasured.

The journal consumes an explicit private root once, holding an exclusive stable lock and preserving initialization markers. It binds a canonical hash of the entire owned compiled configuration and regenerated manifest. Intent is written completely, file-synced, atomically replaced and directory-synced before transport. Only final acknowledgement classification becomes an outcome. Any persistence failure stops later payloads; closing never removes the state or permits retry. Previously used, partial, corrupt or missing-state roots refuse reopening. This is conservative local POSIX behavior, not proof of physical power-loss durability, network-filesystem semantics or malicious same-UID containment.

The combined tests use the actual compiler, driver, adapter and journal with an in-memory HTTP peer. The peer checks on-disk intent before accepting each POST. Tests cover three final outcomes, binding despite caller-owned mapping mutation, restart refusal without another socket, actual fsync failure injection at both file/directory stages before and after sending, and abrupt process exit after persisted intent. These exercise local filesystem operations and a short-lived test child; no configured backend, C2 container, account, credential or native MCP session was used.

## Validation and review

Parent full suite: **560 passed, 36 skipped** in 26.24 seconds. Ruff passed for all six changed Python files. All nine source, contract and test hashes were unchanged across validation. [Sol's independent review](/Users/jasonkrueger/maoi-stage-b-evidence/dispatch/20260921-c2-transport-journal/final-sol-review.md) returned **PASS for bounded source preparation** with no remaining concrete blocker (SHA-256 `89d214dd1502bea18089beec9c36c66a7a8bed7a57daa7984c5cd2e17e613552`). The source is committed locally as `d8da5fc3065a7af9f7ca3b72cd1d5b45fcf35d2c` on `prototype/mcp-grafana-eyes`; staged whitespace checks passed. Existing C1 code and the C2 configuration compiler were unchanged. Review corrections included validating response caps before any POST, rejecting URL controls that Python parsing could otherwise normalize, testing actual fragmented-read deadlines, complete configuration snapshot binding, and rechecking private directory mode/owner while the journal is open.

The initial new-code Kimi review used the wrong provider through `headless opencode --model kimi-code-plan-global/k3 --reasoning-effort high --allow read-only`. Model availability and read-only command expansion were verified first. The one-shot call exited 1 after 8.734 seconds with `APIError: The API Key appears to be invalid or may have expired.` It produced no review. Classification: **authentication failure**. The user then supplied a working `moonshotai/kimi-k3` example. That exact provider/model was verified and a fresh read-only headless combined review was launched, using provider-default effort. No login or credential change was performed. Usage/cost telemetry was unavailable for the failed wrong-provider call and is not treated as zero. The earlier Fable provider-safeguard rejection remains separately incomplete; its packet was not retried or rerouted.

Private preparation/review/validation artifacts are under `/Users/jasonkrueger/maoi-stage-b-evidence/dispatch/20260921-c2-transport-journal/`. The corrected `moonshotai/kimi-k3` call timed out after 300.704 seconds with no final review. Native OpenCode session `ses_f3b1529e5ffep9ahDKrK8cidZD` confirms the selected provider/model but contains only an unfinished assistant record, empty reasoning and unpopulated token/cost fields. Those zero placeholders do not establish zero usage or billing. The read-only native-session summary is retained; CLI export returned truncated JSON, so metadata was read from the exact session in OpenCode's database. The Kimi review remains **incomplete**, with no automatic retry. The wrong-provider authentication failure remains a separate diagnostic artifact.

## Remaining work

Next preparation is operator materialization and ownership/cleanup bookkeeping, Grafana account/token lifecycle, C2 Forwarder/native-case policy, and an exact-resource compatibility card. The HTTP response subset must be checked against the pinned backends before any claim of working ingestion/read-back. Socket deadlines do not preempt arbitrary callbacks, filesystem stalls, or OS suspension; total process containment remains a separate requirement. Retained raw response evidence still needs the operator's private persistence/export lifecycle.

No C2 runtime approval, fifth C1 attempt, model/tenant/venue qualification, ticket resolution or spend-reservation release follows. Ticket 19 remains claimed and ticket 12 remains blocked. All commits remain local.
