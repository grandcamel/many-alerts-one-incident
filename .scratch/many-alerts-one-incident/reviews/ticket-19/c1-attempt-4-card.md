# Ticket 19 — prepared C1 remaining-check card, revised 2026-09-21 UTC

**Preparation only; execution is not yet authorized.** The user's latest “proceed” authorizes the fixture correction and parallel C2 source preparation after the [attempt 3 outcome](c1-attempt-3-outcome.md). It does not reuse attempt 3's consumed runtime approval. No fourth experiment has run.

## Purpose and sample count

Correct the fixture's universal 10 MiB assumption using the actual sampled tool paths, retain the Prometheus counterexample as regression evidence, and reach the remaining negative/direct/final-snapshot checks. One positive native session runs the seven unchanged positive cases as a regression of the rebuilt fixture service. One negative session runs the three Prometheus byte-body observations, three Loki boundary cases, three Tempo boundary cases, then error, redirect, timeout and prior-run denial: **13 cases**, as frozen in the source below. The four additional warm samples are not repeated; attempt 3 retains its timing evidence.

For each tool, bodies are valid tiny JSON padded with trailing whitespace to 10 MiB − 1, 10 MiB and 10 MiB + 1. Loki and Tempo use the pinned shared helper and must reject the oversized body. Prometheus retains its observed successful small decoded result for all three sizes. This checks encoded backend bodies on these specific paths; it does not establish model spill behavior, meaningful large-result fidelity, all-tool limits or whole-process memory bounds. Fixture assessment must preserve exact results below the cap and require the precise helper error above it. Tempo returns the padded body as MCP text, so all three cases reserve body size plus 512 KiB; successful cases require exact text-byte comparison, and the overflow reservation preserves an unexpected uncapped counterexample. The existing 64 MiB frame, 100 MiB bundle and five-second RPC ceilings remain unchanged; a large-frame timeout is a recorded failure, not permission to increase the timeout.

## Inputs, resources and deadlines

Retain the [attempt 3 execution card](c1-execution-card.md)'s exact Docker engine/context/builder, local base image IDs, Linux executable SHA, four Compose roles, project/network/volume identities and scoped temporary image references. Do not pull replacements, reuse live resources or change privileges, mounts, networks, credentials or authority. Check all names absent before creation, then record fresh exact resource IDs. Rebuild the service from the new reviewed fixture; record derived IDs as outputs. The Run binary and probe source are unchanged, but the Run derivative ID must still be read back.

The prepared operator harness is `/Users/jasonkrueger/maoi-stage-b-evidence/c1-prep/attempt-4-execution.py`. Source is frozen at prototype `5cbf616328ec7763a9987a9541c60a0fd6254c10` with 40 file hashes. `/Users/jasonkrueger/maoi-stage-b-evidence/c1-prep/attempt-4-verification.json` has SHA-256 `7acc563d7d46cd5db1a280fb3ffc4143833adb47b23ee6e0c7e59e23d975cfd3`; the closed harness SHA-256 is `7a1f4a5b3bd8fa22689fd2b6383f7f9db66b47c7195a07f218c1f7c02032652d`. The full suite passed 476 tests / 36 skipped and the bound harness passed 24 offline tests. Fable/Sol preparation review and residual findings are recorded in the [review follow-up](c1-c2-review-followup-2026-09-21.md). `EXECUTION_GATE_OPEN = False` refuses execution before any project import or evidence-directory creation, even with the explicit flag. A new execution approval remains required. Its own `--execute-approved-c1-attempt4` flag is an operator action after approval, not evidence of approval by itself.

The host driver, certificate helper, Compose file and build sources are taken from a read-only hash-verified snapshot, with source inventories rechecked before and after use and compared to capture provenance. The closed harness hash is review evidence; any later gate change requires recording the resulting exact executable hash before execution.

Fresh evidence root: `/Users/jasonkrueger/maoi-stage-b-evidence/c1/attempt-4-20260920`. Refuse an existing destination. Reserve two 100 MiB native bundles plus 100 MiB supporting evidence within the unchanged 2 GiB total retained-evidence ceiling. Free disk space must cover the same 300 MiB reservation. Receipt-file limits and 30-day retention stay unchanged. Each native session, RPC and upstream request retains its existing deadline; setup/measurement/cleanup remain 600/600/120 seconds, with measurement additionally bounded before the held namespace expires. Cleanup reserves its first 20 seconds for capture/revocation/export, the next 80 for removal, and the final 20 for a residual/base-image sweep. Critical containers are removed in Forwarder, Run, Gateway, Backend order with exact label checks and bounded inspect/remove calls. Export exhaustion is a failed experiment, not permission to consume removal time. No automatic timeout extension, retry or resource increase is permitted.

## Required checks and stop behavior

Before provisioning secrets, verify exact per-role mount identities, tmpfs options, namespace/network memberships and resource/privilege settings, including the recorded IPC/UTS/user/cgroup namespace, runtime, device-rule and lifecycle fields; repeat that full inventory at the final snapshot. The pinned existing Compose posture has Docker MemorySwap at twice Memory (Run: 2 GiB RAM / 4 GiB combined RAM-plus-swap; services: 256 MiB RAM / 512 MiB combined), as recorded in attempt 3. This card now checks those actual limits explicitly rather than treating the RAM setting as a combined cap. Before admission, repeat baseline parity, filesystem/capability, recomputed network inventory/interface agreement and all twelve exact gateway/backend-IP/private-socket denials. Do not infer these from prior containers. For the cap cases, require the expected datasource lookup plus one exact padded-body response, with unique request IDs, strict response lengths and response hashes; do not mistake the lookup for an extra query. Preserve mismatch correlation records. After each session, require complete unredacted capture, exactly ordered case names and successful fixture flags, zero host transport exit, reaping/closed pipes/no escalation, and absence of native child/wrapper in the Run.

After both sessions pass, perform the previously unrun direct valid/missing/wrong-sentinel and write probes, direct bad-CA/hostname checks with exactly six named, complete and bounded probe results, revocation and post-revocation 401 probes, and final full network/filesystem/identity snapshot. These are direct Run probes; they are not native-client certificate-variant coverage. Native certificate variants, in-container interruption and restart-persistence drills remain separate acceptance gaps. Independently verify full payload hashes, exact service receipt joins or explained denials/errors, and timing from captured events before awarding each claim.

Any fixture mismatch, incomplete/redacted capture, unexpected route/access, leaked credential or missed bound stops further cases. Always attempt bounded revocation, evidence export and removal of the exact created containers/network/volume/derived references, preserving original images. Required receipt exports must succeed and contain no registered secret before sanitization; failures invalidate the evidence. Require another retained base-image reference before temporary tagging and again before untagging; verify original base IDs after cleanup. Record any remaining project container, network, volume or image reference as a cleanup failure. Evidence-write failures during cleanup are recorded in memory and do not suppress removal. Verify cleanup independently. Retain partial failure evidence and list unrun checks; no fifth attempt follows automatically.

Operator evidence hardening now redacts registered raw and canonical JSON-escaped secret forms, including multiline PEM, before structured serialization and terminal summaries. The final scan streams every regular retained file and checks raw, stripped and up to three JSON-escaped forms; this is a bounded known-value scan, not a universal encoding detector. Known-secret matches invalidate evidence and are redacted or quarantined with a failure record; they cannot become a successful clean-evidence claim. Rejection tests cover omitted/reordered/numeric probe verdicts, altered inventories, multiline secrets crossing scan chunks, missing exports, source drift and evidence-disk failure during cleanup. Host process timeout sweeps, default seccomp/CapBnd measurement, hard termination and interruption/restart remain separate unmeasured boundaries. The card requests one positive regression session; additional warm sessions are not required.
