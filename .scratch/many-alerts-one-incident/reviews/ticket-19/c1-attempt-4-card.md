# Ticket 19 — prepared C1 remaining-check card, 2026-09-20

**Preparation only; execution is not yet authorized.** The user's latest “proceed” authorizes the fixture correction and parallel C2 source preparation after the [attempt 3 outcome](c1-attempt-3-outcome.md). It does not reuse attempt 3's consumed runtime approval. No fourth experiment has run.

## Purpose and sample count

Correct the fixture's universal 10 MiB assumption using the actual sampled tool paths, retain the Prometheus counterexample as regression evidence, and reach the remaining negative/direct/final-snapshot checks. One positive native session runs the seven unchanged positive cases as a regression of the rebuilt fixture service. One negative session runs the three Prometheus byte-body observations, three Loki boundary cases, three Tempo boundary cases, then error, redirect, timeout and prior-run denial: **13 cases**, as frozen in the source below; the required Fable review remains outstanding. The four additional warm samples are not repeated; attempt 3 retains its timing evidence.

For each tool, bodies are valid tiny JSON padded with trailing whitespace to 10 MiB − 1, 10 MiB and 10 MiB + 1. Loki and Tempo use the pinned shared helper and must reject the oversized body. Prometheus retains its observed successful small decoded result for all three sizes. This checks encoded backend bodies on these specific paths; it does not establish model spill behavior, meaningful large-result fidelity, all-tool limits or whole-process memory bounds. Fixture assessment must preserve exact results below the cap and require the precise helper error above it. Tempo returns the padded body as MCP text, so its successful cases need an explicit per-case capture reservation and exact text-byte comparison. The existing 64 MiB frame, 100 MiB bundle and five-second RPC ceilings remain unchanged; a large-frame timeout is a recorded failure, not permission to increase the timeout.

## Inputs, resources and deadlines

Retain the [attempt 3 execution card](c1-execution-card.md)'s exact Docker engine/context/builder, local base image IDs, Linux executable SHA, four Compose roles, project/network/volume identities and scoped temporary image references. Do not pull replacements, reuse live resources or change privileges, mounts, networks, credentials or authority. Check all names absent before creation, then record fresh exact resource IDs. Rebuild the service from the new reviewed fixture; record derived IDs as outputs. Run source/binary are unchanged, but the Run derivative ID must still be read back.

The prepared operator harness is `/Users/jasonkrueger/maoi-stage-b-evidence/c1-prep/attempt-4-execution.py`. Source is frozen at `f08f3886fe7ba7c5866c0cf84628b764fde69379` with 38 file bindings in `attempt-4-verification.json` (SHA-256 `10be188e6abc4b8b7c362d77f3e98ac1ec550400f4bcf65f1879c9910ccb64e5`). The final harness SHA-256 is `fb1b40e07cd2f661c2553c457a7ac436cf8d2ca06dcbcdb37b6d0438fc680eac`. Sol reviewed the preparation and final gate delta; the required Fable invocation timed out without a result. `REVIEW_GATE_COMPLETE = False` refuses execution before evidence-directory creation, even with the explicit flag. This is not execution-ready until Fable review completes and a new execution approval is recorded. It requires its own `--execute-approved-c1-attempt4` flag; that flag is an operator action after approval, not evidence of approval by itself.

Fresh evidence root: `/Users/jasonkrueger/maoi-stage-b-evidence/c1/attempt-4-20260920`. Refuse an existing destination. Reserve two 100 MiB native bundles plus 100 MiB supporting evidence within the unchanged 2 GiB total retained-evidence ceiling. Receipt-file limits and 30-day retention stay unchanged. Each native session, RPC and upstream request retains its existing deadline; setup/measurement/cleanup remain 600/600/120 seconds, with measurement additionally bounded before the held namespace expires. No automatic timeout extension, retry or resource increase is permitted.

## Required checks and stop behavior

Before provisioning secrets, verify exact per-role mount identities, tmpfs options, namespace/network memberships and resource/privilege settings; repeat that full inventory at the final snapshot. The pinned existing Compose posture has Docker MemorySwap at twice Memory (Run: 2 GiB RAM / 4 GiB combined RAM-plus-swap; services: 256 MiB RAM / 512 MiB combined), as recorded in attempt 3. This card now checks those actual limits explicitly rather than treating the RAM setting as a combined cap. Before admission, repeat baseline parity, filesystem/capability, recomputed network inventory/interface agreement and all twelve exact gateway/backend-IP/private-socket denials. Do not infer these from prior containers. After each session, require complete unredacted capture, exactly ordered case names and successful fixture flags, zero host transport exit, reaping/closed pipes/no escalation, and absence of native child/wrapper in the Run.

After both sessions pass, perform the previously unrun direct valid/missing/wrong-sentinel and write probes, direct bad-CA/hostname checks with exactly six named, complete and bounded probe results, revocation and post-revocation 401 probes, and final full network/filesystem/identity snapshot. These are direct Run probes; they are not native-client certificate-variant coverage. Native certificate variants, in-container interruption and restart-persistence drills remain separate acceptance gaps. Independently verify full payload hashes, exact service receipt joins or explained denials/errors, and timing from captured events before awarding each claim.

Any fixture mismatch, incomplete/redacted capture, unexpected route/access, leaked credential or missed bound stops further cases. Always attempt bounded revocation, evidence export and removal of the exact created containers/network/volume/derived references, preserving original images. Verify cleanup independently. Retain partial failure evidence and list unrun checks; no fifth attempt follows automatically.

Operator evidence hardening now redacts registered raw and canonical JSON-escaped secret forms, including multiline PEM, before structured serialization and terminal summaries. The final scan streams every regular retained file and checks raw, stripped and up to three JSON-escaped forms; this is a bounded known-value scan, not a universal encoding detector. Rejection tests cover omitted/reordered/numeric probe verdicts, altered inventories and multiline secrets crossing scan chunks.
