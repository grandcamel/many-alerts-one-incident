# C2 file materialization and ownership source outcome — 2026-09-21

Source preparation only; C2 runtime remains NOT RUN. Two Terra lanes implemented one-shot host file staging and pure resource ownership/cleanup selection. The parent connected both to the existing compiler, journal and driver and supplied additional adversarial corrections. The [plan](c2-materialization-ownership-plan.md) fixes the scope; no runtime executor is introduced.

The materializer validates the complete regenerated compiler output and stages five resolved configuration artifacts below a fresh run directory in an explicit private parent. Per-service mount-relative paths are preserved. It uses exclusive creation, no-follow directory descriptors, explicit modes, full writes, file/directory fsync and exact file/tree read-back. Root/journal/evidence directories are private; only nonsensitive service configuration subtrees have future bind-readable modes. The manifest binds the run, complete configuration digest and exact artifact metadata. A failed or existing root is consumed and never reused or removed. A manifest alone is not admission evidence, because an I/O failure may occur after it appears. Actual Docker bind behavior and UID 2000 readability remain untested.

The ownership module plans exact names and labels for four containers and one network and binds a separate plan digest. It accepts explicit caller-supplied successful creation receipts with full IDs, then requires matching present/absent observations. Names and labels never authorize adoption. Missing attachment inspection is distinct from an explicitly empty list. Container IDs may be selected first; the network cannot become a candidate until receipt-backed containers are observed absent and a fresh network observation is empty. It issues no engine commands and deletes nothing. Receipt authenticity, completeness, durability and freshness remain caller responsibilities; an unreceipted crash residue requires manual audit rather than discovery-based adoption. An eventual executor must re-inspect before acting.

Three parent integration tests exercise the actual compiler, materializer, ownership digest, durable journal and driver with an in-memory wire peer. They preserve the public Prometheus compatibility gate, show caller-map mutation cannot retarget prepared artifacts, and prevent receipts from one run qualifying another run with the same configuration. File failure checks include partial-root refusal, replacement/substitution detection and late manifest persistence failures. Review also corrected a FIFO substitution hang by using nonblocking read handles and checking regular-file metadata before any read; the regression proves the unsafe replacement never reaches `os.read`. Ownership tests reject missing network attachment inventory rather than treating it as empty.

The parent full suite passed **583 tests, with 36 skipped**, in 28.36 seconds. Ruff passed for all five new Python files. All seven source/contract/test hashes were unchanged across validation. These are local offline tests, including filesystem fault injection and in-memory transport; they do not qualify actual engines, backends, models or tenants.

Independent [Sol review](/Users/jasonkrueger/maoi-stage-b-evidence/dispatch/20260921-c2-materialization-ownership/final-sol-review.md) returned **PASS for bounded source preparation**, with no remaining blocker; review SHA-256 `def6a5d5f2ad3fb128864491f6644daaf8ae39077f5ed07c9c601b7fd0bb0977`. The earlier Fable cybersecurity safeguard rejection remains incomplete and its packet was not retried or rerouted. A fresh read-only `headless opencode --model moonshotai/kimi-k3` review of these four new source/contract files timed out after 300.495 seconds without a final opinion. The frozen packet was 49,258 bytes. Native session `ses_f3af2d4e8ffeXf8gmtA6t2fCBV` confirms provider/model selection but has an unfinished assistant record with unpopulated usage fields; cost remains unknown, not zero. Classification: **timeout**. No automatic retry or credential change occurred. This is separate from earlier review failures.

Prototype source is committed locally as `6261ac884a9a7897e8f32f9f9e149602f18d2ea6` on `prototype/mcp-grafana-eyes`. All seven committed blobs exactly match the validated and Sol-reviewed hashes; the checkout is clean. The compiler, transport, driver, journal and C1 code were unchanged.

Remaining preparation: durable engine receipt persistence/creation and cleanup execution, exact-resource compatibility card, Grafana account/token lifecycle, C2 Forwarder/native-case policy, private raw-response persistence/export lifecycle, and process containment. Pinned-image configuration, direct binaries, environment, mounts, limits, network/admission and fixed-length HTTP compatibility still need separately authorized runtime evidence. No C2 runtime, fifth C1 attempt, qualification run, ticket resolution or spend-reservation release follows. Ticket 19 stays claimed and ticket 12 blocked. All commits remain local.

Private review, source binding, validation and native-session artifacts: `/Users/jasonkrueger/maoi-stage-b-evidence/dispatch/20260921-c2-materialization-ownership`.
