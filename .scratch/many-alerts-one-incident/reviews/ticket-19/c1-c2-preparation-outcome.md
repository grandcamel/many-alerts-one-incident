# Ticket 19 — C1 cap correction and C2 seed preparation, 2026-09-20

**Source changes saved as work in progress; required Fable review is incomplete. No container experiment executed.** This is the user's approved parallel preparation after [C1 attempt 3](c1-attempt-3-outcome.md). It preserves the Prometheus counterexample, corrects the sampled per-tool fixture contract, prepares deterministic C2 seeds and a constrained startup design, and hardens the next C1 operator harness. Ticket 19 remains claimed; ticket 12 remains blocked.

## Changes and evidence

Two GPT-5.6 Terra workers implemented disjoint source tasks in `/Users/jasonkrueger/projects/maoi-mcp-grafana-prototype`. Source is saved locally at `f08f3886fe7ba7c5866c0cf84628b764fde69379` as work in progress pending Fable review. The prototype checkout is clean. Source validation covers local preparation only. The shared final suite passed **429 tests / 36 skipped**, 465 collected, in 23.47 seconds; the hardened operator harness passed **9 offline tests**, then **10** after adding and independently reviewing the closed Fable gate. No Docker/container, native MCP, real-backend ingestion or qualification-model experiment ran. Primary-source downloads and the separately authorized reviewer session are distinct from experiment execution.

| Work | Result | Remaining acceptance boundary |
| --- | --- | --- |
| C1 response-limit fixture | Three Prometheus observations preserve success at 10 MiB − 1 / = / + 1. Independently named Loki and Tempo cases exercise their shared helper at the same boundaries, requiring an overflow error only at + 1. | Corrected native cases have not executed. The audit covers only the exercised query/log/trace paths, not all 22 inventoried tools or the whole Prometheus client dependency. |
| Capture fidelity | Tempo's successful padded bodies require exact returned text and body size + 512 KiB admission capacity before dispatch. Loki cap fixtures contain exactly one record despite native limit+1 lookahead. | Padded body fidelity is distinct from meaningful large trace results or model spill. Existing 64 MiB frame, 100 MiB bundle, five-second RPC and 60-second native-session ceilings remain. |
| C2 seed generator | Explicit `t0` and `observed_at`; stable Loki push / Prometheus OTLP / Tempo OTLP JSON and hashes; twelve multiline logs, eight traced members, current/prior Change and Run streams, exact metric samples and three linked spans. | No clock selection, HTTP, retry, re-seed or backend acceptance. The response oracle/comparator is incomplete (Loki suffixes, Prometheus timestamp/value pairs, Tempo span IDs only); exact normalized labels/rows/multiline content/trace structure must be frozen and compared before any C2 execution card or acceptance. Actual label translation, ingestion, read-backs, native default-limit mapping and policy/driver/seeder integration remain unimplemented or unmeasured. |
| C2 startup design | Four direct-binary containers from the already pinned image, explicit non-root/read-only configuration, candidate storage/memory/swap/log limits, proposed operator-owned credential and bounded-readiness contracts. | No pull/build/start. Full configs, executable permissions, component startup compatibility and exact resource inventory still need preparation and separate runtime approval. |
| Next C1 harness | One seven-case positive regression session plus one 13-case negative session; then remaining direct/revocation/final checks. Exact case lists, probe results and resource inventories; stronger secret redaction/scan. | [Attempt-4 card](c1-attempt-4-card.md) is prepared, not executed. Source/hash pins are frozen, but `REVIEW_GATE_COMPLETE = False` refuses even the explicit execution flag before any runtime/evidence mutation. Fable review and a new execution approval remain outstanding. Native TLS variants and interruption/restart drills remain separate. |

Prototype contracts: `prototype/local_acceptance/C1_RESPONSE_BODY_CAP_CONTRACT.md` and `C2_SEED_CONTRACT.md`. The [C2 startup design](c2-startup-design.md) records the tagged launcher side effects and exact remaining configuration work. The old launcher’s automatic service-account/token creation, anonymous Admin default, plugin fetching and extra services are not silently admitted into C2.

## Adversarial findings and corrections

Independent GPT-5.6 Sol review found inherited harness gaps: an empty TLS result suffix could pass `all()`, service mount/tmpfs and network inventories were incompletely checked, and raw-value secret matching missed JSON-escaped multiline PEM. The revised harness requires exact six-row direct probes, twelve exact address/socket denials, strict per-role mount/tmpfs/resource/namespace checks before provisioning and at the final snapshot, and raw/stripped/canonical escaped secret checks across all regular retained files. The old RAM limits are now distinguished from their measured Docker RAM-plus-swap allowances. C2 startup requirements now include inspected Docker log rotation/export bounds and no additional swap allowance.

Primary review also corrected the Loki limit+1 interaction, C2 exclusive query-end omission of the twelfth log, a log timestamp outside its assigned span, ambiguous unit suffix translation, and negative/overflow timestamp inputs. Tests independently exercise the specific failure paths. The historical attempt-3 secret-scan claim is qualified in its outcome; a follow-up marker inspection found no matches in 255 retained files, without claiming universal encoding coverage or recovering deleted keys.

Fable was requested as `claude-fable-5-1` through a fresh read-only headless invocation with a 480-second deadline. It **timed out** (exit 124, 481.424 seconds, empty stdout); stderr reports only the timeout. This is not a model opinion or completed review, and no automatic retry was made. Session usage/cost is unavailable, not zero. Sol returned **PASS_WITH_FINDINGS** for preparation, retaining the incomplete C2 response oracle as an execution blocker. The adversarial review requirement therefore remains open.

Both reviews received the frozen packet `/Users/jasonkrueger/maoi-stage-b-evidence/dispatch/20260920-cap-c2-review/prompt.md`, SHA-256 `f2a390971ce9cfca01d9ea4cc8f33cf593da8efc62e230cc2bcab508220f68c7`. Afterward, only the accepted oracle limitation was added to the C2 contract, static source/manifest pins were bound in the private harness, and an explicit closed Fable gate plus refusal test were added. Sol independently reviewed that small final delta and passed it; all ten harness tests pass. These changes do not fill the missing Fable review.


## Private artifacts and next work

Review/source captures and validation logs remain outside Git under `/Users/jasonkrueger/maoi-stage-b-evidence/`. The offline seed example at `c2-prep/20260920-source/offline-seed-example.json` is explicitly historical test input (T0 `2026-09-20T12:00:00Z`, observed `12:03:00Z`), not a fresh seed for a future execution; its SHA-256 is `763c345326e7377784882716d1eb4449d3f8bd058bf6f965857cee8837f14ca4`.

Next, diagnose the headless Claude review path and obtain a usable Fable review of the final source/harness before any C1 execution decision. The timeout alone does not establish whether the model, authentication, backend or wrapper caused the failure. C2 can independently continue with the complete response oracle/comparator and malformed-readback tests, then exact configuration, endpoint/authority policy and operator seeder/driver preparation. No automatic fourth C1 attempt, C2 provisioning, paid qualification, ticket resolution, reservation release, push or publication follows from this source result.

Frozen preparation bindings: 38 source files at `f08f3886fe7ba7c5866c0cf84628b764fde69379`; verification manifest SHA-256 `10be188e6abc4b8b7c362d77f3e98ac1ec550400f4bcf65f1879c9910ccb64e5`; final private harness SHA-256 `fb1b40e07cd2f661c2553c457a7ac436cf8d2ca06dcbcdb37b6d0438fc680eac`. The Fable gate is closed. Logs and final copies are indexed in the private dispatch directory. No raw captures or authority values were committed.
