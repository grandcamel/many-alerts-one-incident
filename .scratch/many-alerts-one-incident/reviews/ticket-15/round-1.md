# Ticket 15 — first decision round accepted

The human answered “agree with all” to the six recommendations. This accepts the planning policy, not runtime implementation or live acceptance.

Constraints already settled: report-only; Ground truth stays repository-only (ADR 0008); ticket 13's Memory is untrusted and rehearsal-scoped; ticket 21 owns refusal/timeout/retry behavior; ticket 12 owns Eyes access. All runtime capability claims need evidence; no live Run or export here.

1. Export scope: recommend operational metrics and structured export events needed for activity, latency, usage and failures; exclude beta traces from the required demo path. Exact native inventory and version compatibility remain verified implementation inputs, not assumed live capabilities.
2. Transcript transport: recommend the Receiver sends a sanitized, structured projection of Transcript Run events to Loki through the collector; preserve event type, timing, Run/session correlation and useful diagnostic content. Raw Transcript bodies do not go straight into shared telemetry. Human-readable rendering is a presentation layer, not the sole stored schema.
3. Run visibility: recommend read-only access to execution telemetry from the current Run and earlier Runs in the same rehearsal for diagnosing its own tool/transport problems. These records are not independent evidence of a system Fault; application claims must trace back to retrieved system evidence. Query-generated telemetry must not cause unbounded recursive investigation.
4. Identity/content policy: recommend dropping account identity and excluding credentials/sentinels, raw prompts and unfiltered tool bodies before shared-stack ingestion, regardless of demo account. Preserve explicit approved diagnostic fields and references; visibly mark omissions. Ground truth/scoring material is never shipped. This is a policy to implement and test, not a claim that existing redaction handles arbitrary secrets.
5. Delivery failures: recommend bounded best-effort export that cannot block Incident work, with visible missing/dropped telemetry status; absence of telemetry never means success. Ticket 21 retains Run success/failure classification and operational retry mechanics; storage/queue limits are downstream specification details.
6. Audience view: recommend a compact per-Run dashboard for activity, elapsed time, usage and outcome plus a linked sanitized event timeline, correlated to Notifications and all touched Incidents. Keep Run telemetry visibly separate from system telemetry; do not imply each Run has exactly one Incident or that a Run's diagnosis is independently verified.

Next frontier after these choices: exact fields/correlation ownership and index labels vs metadata, queue/retention bounds and replay/delivery identity, permission enforcement and sanitized event projections, dashboard acceptance, version-specific exporter verification. Preserve missing/unknown terminal data rather than infer successful completion.
