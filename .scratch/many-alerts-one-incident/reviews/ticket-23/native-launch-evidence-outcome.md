# Native-launch evidence preparation outcome

2026-09-22. Source preparation only. No application code, native client execution,
provider request, account operation or deployment changed.

The [contract](native-launch-evidence-contract.md) and
[readiness record](native-launch-readiness.json) map 14 evidence areas to the
phase and purpose that needs them. They distinguish a proposed provider-disabled
client observation, a metered diagnostic, and representative model qualification.
Actual stream observations, charges and Report verdicts remain post-attempt facts;
their safety, accounting and capture prerequisites must exist before the relevant
attempt. The records grant no admission or launch authority.

The checks retain optional Confluence degradation, early cancellation deadlines,
OPS handling despite audit loss, strict aggregate cost accounting and separate
human Report adjudication. The current synthetic process interval is explicitly
insufficient to establish native timing through durable closeout.

## Verified source evidence

- The current Claude launcher still resolves to the pinned 2.1.278 executable;
  its bytes and digest match. No executable was run during this verification.
- Registered client command outputs, the wrapper entry script and retained
  documented-stream evidence match their source receipts. A wrapper entry hash
  does not freeze its entire dependency tree or establish native behavior.
- All six approved fixture inputs match at historical Git commit
  `79a14c8904f3a125d1f03b192d10797d30979c86`. These are historical Git-object
  references, not missing current-checkout files. The approved rubric also matches.
- The supervised-streaming receipt is verified against its frozen source baseline;
  its recorded full suite remains **874 passed, 36 skipped**. That historical test
  result is not new native or provider evidence.

The [validation record](native-launch-evidence-validation.json) retains exact
hashes, reference checks and review scope. No full test run is needed for this
documentation-only change; application and test code remain unchanged.

## Next scope boundary

The native contracts now require application implementation rather than more
synthetic stream examples. Tickets 36 and 37 explicitly exclude runtime code
from their planning scope, and the standing continuation instruction requires
respecting that distinction. The
[implementation proposal](../native-runtime-source-implementation-proposal.md)
makes the next local source work concrete: scoped Forwarder services and leases,
durable Receiver/accounting state, and a launch path that refuses missing native
readiness. It proposes local code and tests only; separate native, paid, tenant
and deployment gates remain intact.

The planning tickets remain open where their required interface and acceptance
evidence is missing. This record neither resolves them nor alters the approved
rubric, actual spending or human review requirements.
