# Ticket 23 — integrated fixed-client rehearsal

2026-09-21. **FIXED SYNTHETIC CLIENT / NATIVE LAUNCH CLOSED.** Baseline `b5dec5f`.

The [local binding](../../../../prototype/run_timing/TIMING_BINDING.md) and
[integrated runner](../../../../prototype/run_timing/TIMING_REHEARSAL.md) join the
existing synthetic ledger, actual POSIX child supervision, pinned query responses,
Incident revisions/effects and retained snapshots. The actual child performs the work
and publishes its audit/snapshot before emitting their digest receipt. The parent checks
that same attempt's process, receipt, namespaces, request/response history and effects;
it does not substitute an in-parent simulation for missing child evidence.

The model-side local protocol exposes seven reads, candidate lookup and pending
create/append intents. Controller-only completion/capture is outside that dispatcher.
Global call IDs, request size, call capacity, rejection records and detached history are
bounded. The child bundle contains only fixed allowlisted modules and pinned telemetry;
its captured worker hash covers the exact source/data. It contains no rubric or Ground truth.
This trusted scripted fixture is neither a model execution nor an adversarial sandbox.

## Retained final examples

Each scenario has a newly created, independent synthetic fixture ledger. These are test
cases, not reset real accounting or retries of a consumed attempt. Earlier smoke artifacts
remain preserved. Final examples are under the evidence root's `final-examples/`.

| Scenario | Process | Integrated result | Retained effects/revisions | Ledger |
| --- | --- | --- | --- | --- |
| timing_rehearsal | completed | completed | Two confirmed effects, two immutable revisions | One unresolved reservation |
| timing_unknown | completed | held | Unknown-after-apply effect, one revision | One unresolved reservation |
| timing_wait | timed out and reaped | held | Pending dispatch, no effect or revision | One unresolved reservation |

The wait snapshot is explicitly before interruption. It does not claim final revocation
or rollback. A separate deterministic cancellation regression preserves this distinction.
All final examples are read back from disk, match the current fixed worker bytes, and
contain no generated bytecode. Costs and audit/qualification verdicts remain unknown or
NOT_ASSESSED. Fixed Report text exercises storage, not diagnostic judgment.

## Review and corrections

Terra implemented the local binding and fixed client; Luna independently tested them and
reviewed Standards; Terra independently reviewed the integrated Spec. Root assembled the
bundle/supervisor/evidence integration and adjudicated the findings.

- Initial real-child smoke exposed a manifest receipt bug: the worker used the snapshot
  digest instead of the manifest file's digest. It now hashes the actual manifest bytes.
  The failed attempt and its unresolved fixture claim were preserved; later tests are
  separate cases with separate synthetic ledgers.
- Standards found malformed assistant-message and candidate-response objects escaping as
  AttributeError, and candidate call IDs missing the binding identifier validation. Both
  were fixed. Regression work additionally found rehashed malformed candidate items were
  accepted; the closed initial candidate envelope is now checked, including empty items.
- Spec's initial review found no additional blocker. Fable independently required the same
  malformed-object correction. Its optional findings led to disabled bytecode writes,
  explicit mixed-stderr failure semantics and a documented derived history-size ceiling.
  QueryRejected already inherits ValueError and was covered by the normalization boundary.
- Final Standards review passed all 44 focused tests. It identified a conditional capture
  limitation: valid JSON from stderr cannot be distinguished after merging. The contract
  now states that limit explicitly; no claim of absent stderr or stream authenticity is
  made. The tested rejection covers non-JSON/interleaved capture, not every stderr byte.
- Bundle-build failure after the durable claim conservatively preserves unresolved
  accounting. There is no automatic retry or zero-cost inference. Historical read-back
  verifies captured bytes, not a reconstruction using potentially changed current source.

Fable reviewed the frozen initial source through the approved headless review path; it
reported `claude-fable-5-1` and a **$2.818952 session estimate**, with no provider actual.
Its verdict was CHANGES_REQUIRED. Corrections were verified by the local review/test
cycle; Fable was not rerun. This was source review, not a timing/length experiment.

[Validation](integration-validation.json) records final test counts, source hashes,
review disposition and external evidence inventory. The final full suite passed **679 tests, 36 skipped in 39.57s** after all code changes.
The two new modules contain **44 tests**. Ruff and diff whitespace checks passed. Raw review/test/example artifacts remain
outside Git at `/Users/jasonkrueger/maoi-ticket23-evidence/20260921-integrated-rehearsal/`.
The prior packet and its 75 indexed files were archived from `b5dec5f` and byte-verified;
historical receipts remain unchanged. The new packet manifest continues that chain.

## Human approval and remaining work

The user approved the pinned synthetic baseline and rubric. The
[approval receipt](timing-draft/operator/rubric-approval.json) preserves exact original
rubric bytes/hash and historical fixture identity. It supersedes pre-approval draft labels
only. It neither authorizes a model experiment nor grades a future Report.

The existing wrapper transcript has now been inventoried for native-event preparation.
It includes event shapes and an oversized line rejected by the synthetic parser, and
contains no tool-use, cancellation, error-terminal or fallback coverage. This is a
compatibility gap to resolve, not evidence that the parser can accept native streams.
Native transport/process isolation, authenticated effects, provider accounting,
production audit custody/retention and full durable-closeout deadlines remain separate
implementation/acceptance work. Ticket 36 also needs exact Eyes/Confluence policies.
No live Jira, credential/infrastructure mutation, paid timing/length probe, human Report
grade, model/tenant/venue qualification, push or publication occurred. Ticket 23 stays open.
