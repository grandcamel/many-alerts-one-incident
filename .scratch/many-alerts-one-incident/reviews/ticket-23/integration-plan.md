# Ticket 23 integrated fixed-client rehearsal plan

Baseline b5dec5f. User authorizes aggressive local continuation and cheaper model delegation;
the pinned synthetic baseline/rubric is now explicitly human approved. No actual timing/length
model experiment, credential mutation, container/tenant operation or publication is authorized.

1. Terra implements a bounded in-process capability dispatcher over existing query/Incident
   adapters. Luna independently tests exact schemas, identity, rejected/duplicate/capped calls,
   separation of controller-only completion and audit, and retained request/response history.
   All admission is trusted same-process fixture logic, not an authenticated native boundary.
2. Build a fixed scripted client in a closed self-contained worker bundle. Embed only reviewed
   source modules and pinned fixture data, never rubric/Ground truth/account/config. The bundle
   is captured under the existing fixture.py digest; isolated Python runs it through the actual
   fixed process supervisor. No arbitrary executable, callback, route or caller path is added.
3. The scripted child must actually perform query/create/append calls, controller-simulated
   effects and snapshot publication before emitting a digest receipt. Include completed,
   unknown-effect and waiting/cancellation scenarios. Keep child virtual observations distinct
   from supervisor elapsed time; timeout/revocation is not rollback.
4. Compose durable synthetic reservation/claim with that real fixed process launch, then
   read back process evidence, snapshot, binding history and exact capture receipt linkage.
   Keep capture/effect/ledger outcomes separate. Never auto-reconcile missing cost to zero,
   relaunch consumed identity or erase partial failures. Receipts remain synthetic, not auth.
5. Exercise normal flow, uncertain effects, real deadline/cancellation, capture/publication
   loss, malformed/missing/tampered linkage and collisions. Test full suite after final edits;
   run independent Standards/Spec and required Fable source review. Commit locally only.
6. Continue source/native-adapter and runtime-gate preparation where the existing authorization
   permits it. Prepare any future execution card fully before asking for its final approval.

Prior 75-file packet is archived and hash-verified outside Git. The user's rubric approval
record pins original rubric bytes and fixture hashes, superseding draft status only. Actual
human Report grading, native auth/client compatibility, authoritative billing and qualification
remain separate gates. Preserve unrelated ticket19 C2 changes and its rejected-task boundary.
