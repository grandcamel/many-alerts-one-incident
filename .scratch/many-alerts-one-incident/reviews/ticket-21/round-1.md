# Ticket 21 — first decision round accepted

The human answered “Agree” to all five recommendations. These are planning decisions; no runtime or Skill change is authorized.

Accepted constraints: no live mutation probes; ticket 16 owns compact Report shape; ticket 14 owns Match/member/correction/completion rules; ticket 31 uses latest admitted per-source-group state for successful admission; ADR 0009 separates confirmed OPS effects from optional Memory failure; ADR 0010 never infers success from telemetry silence; ADR 0011 forbids blind uncertain-mutation retries and requires revocation on exit/timeout. This is the accepted planning policy, not current behavior.

1. Outcome truth. Track execution outcome separately from external effects. Receiver-owned timeout/cancellation/spawn failure wins over apparent result success. Treat a valid terminal error indicator (including is_error) as failure even with success subtype/exit 0; malformed, missing or contradictory terminal evidence is incomplete/unknown, never success. A normal clean termination with a valid non-error result supports completed execution, not proof that intended Jira/Confluence effects occurred. Confirmed/partial/uncertain/no-effect dispositions need operation evidence. Model unavailability is reported at the observed model/service scope, not generalized to an API-wide outage or a cost of zero inferred from missing usage.
2. Hands refusal. Never create junk Incidents or probe permissions with mutations. If denial proves the intended mutation was not dispatched, permit one shorter attempt carrying a minimal evidence-backed Report with explicit omissions, within the existing Run budget; do not loop or change tool/permission mode. An uncertain dispatch instead requires reconciliation, not a shorter duplicate. Ticket 16 chooses the actual Report structure/size budget; the historical command-length observations are not a universal CLI limit.
3. Deadline handling. Use an overall wall-clock budget covering startup through teardown, reserving a bounded shutdown allowance inside that total. At operator cancellation or the work deadline, revoke service sentinels, request interruption, allow bounded time only for local result/usage flush and process cleanup, then kill/reap the whole process group if necessary. Do not promise interruption produces a final result or rollback. Total allowed elapsed time and grace duration are the next dependent decision; descendant pipe holders must not defeat the bound.
4. Silence and progress. Silence is not proof of a stall. Display elapsed time and time since last observable activity, mark activity as unknown when telemetry is missing, and permit an explicit operator cancel. Do not reset the overall deadline on new output or terminate solely because a reasoning interval is quiet. Output/error observations are evidence for diagnostics, not a substitute for the deadline.
5. Recovery and fallback. After failed/interrupted/incomplete execution or an uncertain required OPS effect, hold new Run dispatch for an explicit operator retry/resume decision while continuing bounded Notification admission/coalescing. Keep the failed work identifiable and reconcile external effects before retry; never replay confirmed writes or auto-switch models. Preserve latest-admitted duplicate semantics but do not let dedupe erase outstanding failed work. Optional Memory/export failures alone do not turn confirmed OPS work into a reason to repeat it. Exact failed/pending merge, restart record and operator controls are the next dependent decisions.

Next frontier after these answers: execution/grace budget, result schema precedence/conflicting terminal records, minimum partial-effect receipts and their lifetime, failed-work/pending merge with repeat baseline, bounded retry/operator-resume actions and diagnostic presentation. No Skill code or live probe is authorized here.
