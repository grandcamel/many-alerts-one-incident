# Ticket 40 — fallback venue evidence

Offline review of committed `prototype/cascade-timing` snapshot `557153b`; no new system/model execution. Historical measurements remain historical and do not qualify a model.

- **Trigger/config observation:** `prototype/cascade-timing/capture/cascade-adFailure-on.log:65-84` records `adFailure` changing `off` to `on`, the ConfigMap resource version changing, flagd rollout and its in-pod default variant read-back. That proves the recorded configuration state, not every application's evaluation. Lines 87-89 explicitly distinguish provider configuration from application behavior.
- **Measured symptoms:** `capture/symptom-adFailure.json:1-29` reports a 307-second observation window: ad error span metrics and an errored ad trace first seen at t+6s, frontend error metrics at t+132s, and no matching ad error-log observation. The detailed log at `capture/cascade-adFailure-on.log:182-185` records five errored ad traces and zero matching log lines at t+275s. A missing match in that query/window is not proof no possible log exists. Trace counts alone do not supply response bodies or exact failure status/message for a new citation audit.
- **Undo:** `capture/cascade-adFailure-on.log:215-246` records `on` to `off`, flagd rollout/read-back and a separate ad Deployment restart. It documents the executed recovery recipe; it does not prove restarting ad is intrinsically necessary under every provider/client condition.
- **Original alert behavior:** the log's rule summary at `:296-305,319-329` puts first Alert firing at t+750.7s, after undo at t+607s. The original `>0.1/s` rule had no in-Fault firing. This later Alert must not be counted as an injection-time Cascade or evidence that the corrected threshold has been tested live.
- **Offline threshold evaluation:** decompressed `capture/alertwatch-adFailure.jsonl.gz` has 309 records. Restricting `kind=replay` to the half-open injection/undo interval using `capture/t0-adFailure.txt:1` and `capture/tundo-adFailure.txt:1` yields 40 C1 sample records. Comparing `cascade-service-error-rate.verdict.values` with `>0.03` gives 31 frontend and 19 frontend-proxy positive samples. First/last positive records: frontend decompressed lines 57/177 (t+144.976s / t+595.543s), frontend-proxy lines 89/177 (t+265.101s / t+595.543s). Their maximum values are 0.07058685/s and 0.03333347/s respectively. No other service's samples cross 0.03 in that interval. This is a comparison of recorded instantaneous rule inputs, not a replay of Grafana's full pending/state machine or a future two-Alert guarantee.

The causal Mechanism still needs pinned source or sufficiently detailed diagnostic artifacts. Do not infer exact RPC code, response content, universal request failure or customer-visible impact from these metric/trace-count observations alone. Corrected-rule live acceptance and current model qualification remain unperformed.
