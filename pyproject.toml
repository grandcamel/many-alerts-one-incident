[project]
name = "grafana-jsm-sandbox"
version = "0.1.0"
description = "Grafana alert Notification -> sandboxed Claude Run -> Jira OPS Incident"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0", "pyyaml>=6.0"]

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]

[tool.ruff]
line-length = 100
